# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'
source 'https://cdn.cocoapods.org/'

install! 'cocoapods',
generate_multiple_pod_projects: true

target 'SwiftUI_App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUI_App
    pod 'Moya', :project_name => 'Networking'
    pod 'Alamofire', :project_name => 'Networking'
    
    pod 'Alamofire', :project_name => 'Networking'

  target 'SwiftUI_AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftUI_AppUITests' do
    # Pods for testing
  end

end

target 'WatchLandmarks' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WatchLandmarks

 end

target 'WatchLandmarks Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WatchLandmarks Extension

end
